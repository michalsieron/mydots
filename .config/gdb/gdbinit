# so there is no paging
set width 0
set height 0

# pretty printing
set print pretty on

# don't need to confirm exit
set confirm off

# show assembly without TUI mode
set disassemble-next-line on
show disassemble-next-line

# turn history on
set history save on
set history filename ~/.gdb_history
set history size unlimited
